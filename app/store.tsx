import { StoreScreen } from '@/online/ui';

/** Mağaza route'u (/store). Sekmeli; şimdilik Sinyaller dolu. Tüm mantık
 *  StoreScreen'de; sahiplik/Veri sunucudan (get_my_rank), satın alma unlock_signal. */
export default function StoreRoute() {
  return <StoreScreen />;
}
