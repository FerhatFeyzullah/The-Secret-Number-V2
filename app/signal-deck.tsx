import { SignalDeckScreen } from '@/online/ui';

/** Sinyallerim / deste route'u (/signal-deck). Sahip olunan sinyallerden ≤6'lık
 *  kalıcı deste; set_signal_deck ile sunucu otoriteli kaydedilir. */
export default function SignalDeckRoute() {
  return <SignalDeckScreen />;
}
